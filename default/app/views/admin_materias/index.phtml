<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-dark m-0">Administración de Materias</h2>
        <a href="<?= PUBLIC_PATH ?>admin_materias/guardar" class="btn btn-uno rounded-3 shadow-sm">
             Nuevo Materia
        </a>
    </div>

    <?php View::content(); ?>

    <div class="card card-login bg-white border-0 shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4">Código</th>
                            <th>Nombre de la Materia</th>
                            <th>Régimen</th>
                            <th class="text-end pe-4">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($listMaterias as $item): ?>
                        <tr>
                            <td class="ps-4 fw-semibold text-secondary"><?= $item->codigo ?></td>
                            <td class="fw-bold text-dark"><?= $item->nombre ?></td>
                            <td>
                                <?php if ($item->promocionable == 1): ?>
                                    <span class="badge bg-success rounded-pill">Promocionable</span>
                                <?php else: ?>
                                    <span class="badge bg-secondary rounded-pill">Solo Final</span>
                                <?php endif; ?>
                            </td>
                            <td class="text-end pe-4">
                                <a href="<?= PUBLIC_PATH . 'admin_materias/correlatividades/' . $item->id ?>" class="btn btn-sm btn-outline-primary rounded-2 me-1">
                                    Correlativas
                                </a>
                                <a href="<?= PUBLIC_PATH . 'admin_materias/guardar/' . $item->id ?>" class="btn btn-sm btn-outline-secondary rounded-2 me-1">
                                    Editar
                                </a>
                                <a href="<?= PUBLIC_PATH . 'admin_materias/eliminar/' . $item->id ?>" class="btn btn-sm btn-outline-danger rounded-2" onclick="return confirm('¿Seguro querés eliminar esta materia?')">
                                    Borrar
                                </a>
                            </td>
                        </tr>
                        <?php endforeach; ?>
                        <?php if (count($listMaterias) == 0): ?>
                        <tr>
                            <td colspan="4" class="text-center py-4 text-muted">No hay materias registradas.</td>
                        </tr>
                        <?php endif; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>