<div class="container mt-4" style="max-width: 700px;">
    <div class="mb-3">
        <a href="<?= PUBLIC_PATH ?>admin_materias" class="text-decoration-none text-secondary small fw-bold">
            ← Volver al listado de materias
        </a>
    </div>

    <div class="card card-login bg-white p-4 border-0 shadow-sm mb-4">
        <h4 class="fw-bold text-dark mb-1">Correlatividades para:</h4>
        <h3 class="text-uno fw-bold mb-4"><?= $materia->codigo ?> - <?= $materia->nombre ?></h3>

        <?php View::content(); ?>

        <form action="<?= PUBLIC_PATH . 'admin_materias/correlatividades/' . $materia->id ?>" method="POST" class="row g-2 align-items-end">
            <div class="col-md-9">
                <label class="form-label small fw-semibold text-secondary">Seleccionar Materia Requerida</label>
                <select name="correlativa_id" class="form-select form-control-lg rounded-3 fs-6" required>
                    <option value="" disabled selected>-- Elegí una materia --</option>
                    <?php foreach ($todasLasMaterias as $mat): ?>
                        <option value="<?= $mat->id ?>"><?= $mat->codigo ?> - <?= $mat->nombre ?></option>
                    <?php endforeach; ?>
                </select>
            </div>
            <div class="col-md-3">
                <button type="submit" class="btn btn-uno w-100 rounded-3 py-2.5 fs-6 shadow-sm">
                    Asignar
                </button>
            </div>
        </form>
    </div>

    <div class="card card-login bg-white border-0 shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4">Código Requisito</th>
                            <th>Materia Requisito</th>
                            <th class="text-end pe-4">Acción</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($listCorrelativas as $item): ?>
                        <tr>
                            <td class="ps-4 fw-semibold text-secondary"><?= $item->codigo ?></td>
                            <td class="fw-bold text-dark"><?= $item->nombre ?></td>
                            <td class="text-end pe-4">
                                <a href="<?= PUBLIC_PATH . 'admin_materias/eliminar_correlativa/' . $item->id . '/' . $materia->id ?>" 
                                   class="btn btn-sm btn-outline-danger rounded-2" 
                                   onclick="return confirm('¿Seguro querés quitar esta correlatividad?')">
                                    Quitar
                                </a>
                            </td>
                        </tr>
                        <?php endforeach; ?>
                        <?php if (count($listCorrelativas) == 0): ?>
                        <tr>
                            <td colspan="3" class="text-center py-4 text-muted">Esta materia no tiene correlativas asignadas.</td>
                        </tr>
                        <?php endif; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>