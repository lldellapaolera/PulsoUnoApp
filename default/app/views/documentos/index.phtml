<div class="row mb-3">
    <div class="col-12 text-center py-2">
        <h4 class="fw-bold text-dark mb-1">📂 Repositorio de Apuntes</h4>
        <p class="text-secondary small">Material de estudio y apuntes de cátedra offline.</p>
    </div>
</div>

<div class="card border-0 shadow-sm rounded-4 mb-4 bg-white">
    <div class="card-body p-3">
        <label for="selector-materia-doc" class="form-label small fw-bold text-secondary mb-1">Seleccionar Asignatura:</label>
        <select id="selector-materia-doc" class="form-select border-light-subtle bg-light">
            <option value="">-- Elija la materia para explorar documentos --</option>
            <?php foreach ($materias_validas as $mat): ?>
                <option value="<?= $mat->comision_id ?>" <?= ($comision_seleccionada && $comision_seleccionada->comision_id == $mat->comision_id) ? 'selected' : '' ?>>
                    <?= htmlspecialchars($mat->materia_nombre) ?> (Comisión #<?= $mat->comision_id ?>)
                </option>
            <?php endforeach; ?>
        </select>
    </div>
</div>

<?php if ($comision_seleccionada): ?>
<div class="card border-0 shadow-sm rounded-4 mb-4">
    <div class="card-body p-4">
        
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h5 class="fw-bold mb-0 text-dark"><?= htmlspecialchars($comision_seleccionada->materia_nombre) ?></h5>
                <span class="text-muted small">Ruta: / <?= htmlspecialchars($ruta_relativa) ?></span>
            </div>

            <?php if ($rolUsuario === 'profesor' || $rolUsuario === 'administrador'): ?>
            <div class="d-flex gap-1">
                <button class="btn btn-outline-primary btn-sm rounded-3 fw-bold" data-bs-toggle="modal" data-bs-target="#modalCarpeta">📁 +</button>
                <button class="btn btn-primary btn-sm rounded-3 fw-bold text-white shadow-sm" data-bs-toggle="modal" data-bs-target="#modalSubir" style="background-color: #0096d6; border-color: #0096d6;">📤 Subir</button>
            </div>
            <?php endif; ?>
        </div>

        <div class="list-group list-group-flush border rounded-3">
            
            <?php if ($ruta_relativa !== ''): ?>
                <a href="<?= PUBLIC_PATH ?>documentos/index/<?= $comision_seleccionada->comision_id ?>" class="list-group-item list-group-item-action py-2 px-3 bg-light-subtle small text-primary fw-semibold">
                    ↩️ Volver al Directorio Principal
                </a>
            <?php endif; ?>

            <?php if (count($elementos) > 0): ?>
                <?php foreach ($elementos as $elem): ?>
                    <div class="list-group-item d-flex justify-content-between align-items-center py-3 px-3">
                        <div class="d-flex align-items-center overflow-hidden">
                            <span class="fs-4 me-3"><?= $elem['es_carpeta'] ? '📁' : '📄' ?></span>
                            <div class="text-truncate">
                                <?php if ($elem['es_carpeta']): ?>
                                    <a href="<?= PUBLIC_PATH ?>documentos/index/<?= $comision_seleccionada->comision_id ?>/<?= urlencode(($ruta_relativa ? $ruta_relativa.'/' : '').$elem['nombre']) ?>" class="fw-bold text-dark text-decoration-none text-truncate d-block">
                                        <?= htmlspecialchars($elem['nombre']) ?>
                                    </a>
                                <?php else: ?>
                                    <a href="<?= PUBLIC_PATH ?>uploads/apuntes/comision_<?= $comision_seleccionada->comision_id ?>/<?= $ruta_relativa ? $ruta_relativa.'/' : '' ?><?= $elem['nombre'] ?>" download class="fw-semibold text-secondary text-decoration-none text-truncate d-block" style="font-size: 0.9rem;">
                                        <?= htmlspecialchars($elem['nombre']) ?>
                                    </a>
                                <?php endif; ?>
                                <span class="text-muted d-block" style="font-size: 0.68rem;"><?= $elem['fecha'] ?> • Peso: <?= $elem['peso'] ?></span>
                            </div>
                        </div>

                        <?php if ($rolUsuario === 'profesor' || $rolUsuario === 'administrador'): ?>
                            <a href="<?= PUBLIC_PATH ?>documentos/eliminar?comision_id=<?= $comision_seleccionada->comision_id ?>&nombre=<?= urlencode($elem['nombre']) ?>&ruta_actual=<?= urlencode($ruta_relativa) ?>" onclick="return confirm('¿Seguro que querés eliminar este elemento de forma permanente?');" class="btn btn-link text-danger text-decoration-none p-2 fs-5">
                                🗑️
                            </a>
                        <?php endif; ?>
                    </div>
                <?php endforeach; ?>
            <?php else: ?>
                <div class="text-center py-4 text-muted small">La carpeta se encuentra vacía.</div>
            <?php endif; ?>
        </div>

    </div>
</div>

<?php if ($rolUsuario === 'profesor' || $rolUsuario === 'administrador'): ?>
<div class="modal fade" id="modalCarpeta" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-sm">
        <div class="modal-content rounded-4 border-0 shadow">
            <form action="<?= PUBLIC_PATH ?>documentos/crear_carpeta" method="POST">
                <input type="hidden" name="comision_id" value="<?= $comision_seleccionada->comision_id ?>">
                <input type="hidden" name="ruta_actual" value="<?= htmlspecialchars($ruta_relativa) ?>">
                <div class="modal-header border-0 pb-0">
                    <h6 class="fw-bold text-dark mb-0">Nueva Carpeta</h6>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body py-3">
                    <input type="text" name="nombre_carpeta" class="form-control rounded-3" placeholder="Nombre de la carpeta..." required>
                </div>
                <div class="modal-footer border-0 pt-0">
                    <button type="submit" class="btn btn-primary w-100 rounded-3 text-white fw-bold">Crear</button>
                </div>
            </form>
        </div>
    </div>
</div>

<div class="modal fade" id="modalSubir" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-sm">
        <div class="modal-content rounded-4 border-0 shadow">
            <form action="<?= PUBLIC_PATH ?>documentos/subir_archivo" method="POST" enctype="multipart/form-data">
                <input type="hidden" name="comision_id" value="<?= $comision_seleccionada->comision_id ?>">
                <input type="hidden" name="ruta_actual" value="<?= htmlspecialchars($ruta_relativa) ?>">
                <div class="modal-header border-0 pb-0">
                    <h6 class="fw-bold text-dark mb-0">Subir Archivo de Cátedra</h6>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body py-3">
                    <input type="file" name="documento" class="form-control rounded-3" required>
                    <span class="text-muted d-block mt-1" style="font-size: 0.65rem;">Formatos admitidos: PDF, Word, Excel, PowerPoint, ZIP e imágenes.</span>
                </div>
                <div class="modal-footer border-0 pt-0">
                    <button type="submit" class="btn btn-success w-100 rounded-3 text-white fw-bold">Subir al Repositorio</button>
                </div>
            </form>
        </div>
    </div>
</div>
<?php endif; ?>

<?php else: ?>
    <div class="alert alert-light border border-light-subtle rounded-4 text-center p-4 shadow-sm bg-white">
        <span class="fs-1 mb-2 d-block">📂</span>
        <h6 class="fw-bold text-dark">Explorador de Archivos Inactivo</h6>
        <p class="text-muted small mb-0">Seleccione una asignatura del listado superior para descargar las guías, lecturas y trabajos prácticos cargados por el cuerpo docente.</p>
    </div>
<?php endif; ?>

<script>
document.getElementById('selector-materia-doc').addEventListener('change', function() {
    const id = this.value;
    window.location.href = id ? "<?= PUBLIC_PATH ?>documentos/index/" + id : "<?= PUBLIC_PATH ?>documentos";
});
</script>