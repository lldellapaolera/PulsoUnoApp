<div class="container mt-4" style="max-width: 600px;">
    <div class="card card-login bg-white p-3">
        <div class="card-body">
            <h3 class="fw-bold text-dark mb-4"><?= $comision->id ? 'Editar Comisión' : 'Nueva Comisión' ?></h3>

            <form action="<?= PUBLIC_PATH ?>admin_comisiones/guardar" method="POST">
                
                <input type="hidden" name="comision[id]" value="<?= $comision->id ?>">

                <div class="mb-3">
                    <label class="form-label small fw-semibold text-secondary">Materia</label>
                    <select name="comision[materia_id]" class="form-select form-control-lg rounded-3 fs-6" required>
                        <option value="" disabled selected>-- Seleccionar Materia --</option>
                        <?php foreach ($listMaterias as $mat): ?>
                            <option value="<?= $mat->id ?>" <?= $comision->materia_id == $mat->id ? 'selected' : '' ?>><?= $mat->codigo ?> - <?= $mat->nombre ?></option>
                        <?php endforeach; ?>
                    </select>
                </div>

                <div class="mb-3">
                    <label class="form-label small fw-semibold text-secondary">Profesor a Cargo</label>
                    <select name="comision[profesor_id]" class="form-select form-control-lg rounded-3 fs-6" required>
                        <option value="" disabled selected>-- Seleccionar Profesor --</option>
                        <?php foreach ($listProfesores as $prof): ?>
                            <option value="<?= $prof->id ?>" <?= $comision->profesor_id == $prof->id ? 'selected' : '' ?>><?= $prof->apellido . ', ' . $prof->nombre ?></option>
                        <?php endforeach; ?>
                    </select>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label small fw-semibold text-secondary">Cuatrimestre</label>
                        <select name="comision[cuatrimestre]" class="form-select form-control-lg rounded-3 fs-6" required>
                            <option value="1" <?= $comision->cuatrimestre == 1 ? 'selected' : '' ?>>1° Cuatrimestre</option>
                            <option value="2" <?= $comision->cuatrimestre == 2 ? 'selected' : '' ?>>2° Cuatrimestre</option>
                        </select>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label class="form-label small fw-semibold text-secondary">Año</label>
                        <input type="number" name="comision[anio]" class="form-control form-control-lg rounded-3 fs-6" required value="<?= $comision->anio ? $comision->anio : date('Y') ?>">
                    </div>
                </div>

                <div class="mb-4">
                    <label class="form-label small fw-semibold text-secondary">Días y Horarios</label>
                    <input type="text" name="comision[dias_horarios]" class="form-control form-control-lg rounded-3 fs-6" required placeholder="Ej: Lunes y Miércoles 18:00 a 22:00" value="<?= $comision->dias_horarios ?>">
                </div>

                <div class="mb-4">
                    <label class="form-label small fw-semibold text-secondary">Sede y aula</label>
                    <input type="text" name="comision[sede_aula]" class="form-control form-control-lg rounded-3 fs-6" required placeholder="Ej: Sede Córdoba LAB 5" value="<?= $comision->sede_aula ?>">
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-uno w-100 rounded-3 py-2.5 fs-6 shadow-sm">
                        Guardar Comisión
                    </button>
                    <a href="<?= PUBLIC_PATH ?>admin_comisiones" class="btn btn-light w-50 rounded-3 py-2.5 fs-6 border">
                        Cancelar
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>