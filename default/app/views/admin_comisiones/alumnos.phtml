<div class="container mt-4" style="max-width: 750px;">
    <div class="mb-3">
        <a href="<?= PUBLIC_PATH ?>admin_comisiones" class="text-decoration-none text-secondary small fw-bold">
            ← Volver al listado de comisiones
        </a>
    </div>

    <div class="card card-login bg-white p-4 border-0 shadow-sm mb-4">
        <h4 class="fw-bold text-dark mb-1">Alumnos Inscritos en:</h4>
        <h3 class="text-uno fw-bold mb-4"><?= $comision->materia_nombre ?> <span class="fs-5 text-secondary">(Prof: <?= $comision->prof_apellido ?>)</span></h3>

        <?php View::content(); ?>

        <form action="<?= PUBLIC_PATH . 'admin_comisiones/alumnos/' . $comision->id ?>" method="POST" class="row g-2 align-items-end">
            <div class="col-md-9">
                <label class="form-label small fw-semibold text-secondary">Seleccionar Alumno para Inscribir</label>
                <select name="alumno_id" class="form-select form-control-lg rounded-3 fs-6" required>
                    <option value="" disabled selected>-- Elegí un alumno --</option>
                    <?php foreach ($listAlumnosDisponibles as $al): ?>
                        <option value="<?= $al->id ?>"><?= $al->id ?> - <?= $al->apellido . ', ' . $al->nombre ?> (Legajo: <?= $al->legajo ?>)</option>
                    <?php endforeach; ?>
                </select>
            </div>
            <div class="col-md-3">
                <button type="submit" class="btn btn-uno w-100 rounded-3 py-2.5 fs-6 shadow-sm">
                    Inscribir
                </button>
            </div>
        </form>
    </div>

    <div class="card card-login bg-white border-0 shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4">Legajo</th>
                            <th>Alumno</th>
                            <th>Email</th>
                            <th>Estado</th>
                            <th class="text-end pe-4">Acción</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach ($listInscritos as $item): ?>
                        <tr>
                            <td class="ps-4 fw-semibold text-secondary"><?= $item->legajo ?></td>
                            <td class="fw-bold text-dark"><?= $item->apellido . ', ' . $item->nombre ?></td>
                            <td class="small text-secondary"><?= $item->email ?></td>
                            <td>
                                <span class="badge bg-info rounded-pill text-capitalize">
                                    <?= $item->estado_materia ?>
                                </span>
                            </td>
                            <td class="text-end pe-4">
                                <a href="<?= PUBLIC_PATH . 'admin_comisiones/calificar/' . $item->id ?>" 
                                    class="btn btn-sm btn-outline-success rounded-2 me-1">
                                        📝 Calificar
                                </a>
                                <a href="<?= PUBLIC_PATH . 'admin_comisiones/desinscribir/' . $item->id . '/' . $comision->id ?>" 
                                   class="btn btn-sm btn-outline-danger rounded-2" 
                                   onclick="return confirm('¿Seguro querés dar de baja la inscripción de este alumno?')">
                                    Dar de Baja
                                </a>
                            </td>
                        </tr>
                        <?php endforeach; ?>
                        <?php if (count($listInscritos) == 0): ?>
                        <tr>
                            <td colspan="5" class="text-center py-4 text-muted">No hay alumnos inscritos en esta comisión aún.</td>
                        </tr>
                        <?php endif; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>